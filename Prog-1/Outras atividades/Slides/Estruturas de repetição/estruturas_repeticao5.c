#include <stdio.h>

int main() {
    double c = 1.5, z = 1.1;

    do {
        z = z + 0.3;
        c = c + 0.2;
        printf("ira demorar %.1f ano para que ze seja maior que chico", z = c);
    } while ( z < c );

    return 0;
}